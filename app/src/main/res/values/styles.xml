<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="progresswheel_custom_style">
        <item name="barLength">100dp</item>
        <item name="barWidth">5dp</item>
        <item name="gradientBar">true</item>
        <item name="android:layout_width">90dp</item>
        <item name="android:layout_height">90dp</item>
        <item name="gradientBarEndColor">@color/white</item>
        <item name="gradientBarStartColor">@color/button_recharge_cyan</item>
        <item name="rimWidth">5dp</item>
        <item name="spinSpeed">2dp</item>
        <item name="text">@string/loading</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="textColor">#222222</item>
        <item name="textSize">10sp</item>
    </style>

    <style name="progresswheel_custom_style_profile">
        <item name="barLength">100dp</item>
        <item name="barWidth">3dp</item>
        <item name="gradientBar">true</item>
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="gradientBarEndColor">@color/white</item>
        <item name="gradientBarStartColor">@color/button_recharge_cyan</item>
        <item name="rimWidth">5dp</item>
        <item name="spinSpeed">2dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="textColor">#222222</item>
        <item name="textSize">10sp</item>
    </style>

</resources>
